# SongADay PFP Builder API

This is a image layer assembly and IPFS publishing API designed to be used with the SongADay PFP Builder.

## Setup

1. Create a `.env` file by duplicating `.env.example`.
